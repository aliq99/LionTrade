app = "liontrade-bot"
primary_region = "sea"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

[processes]
  worker = "python -m bot"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
